apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: intake-assistant
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/seongmin15/sdwc-platform
    targetRevision: main
    path: manifests/intake
  destination:
    server: https://kubernetes.default.svc
    namespace: intake
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
